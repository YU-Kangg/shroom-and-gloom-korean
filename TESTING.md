# 검증 기록 — v0.1.1-beta.1

## 카드 글자 표시 수정

- `TextMeshPro.GenerateTextMesh` 실행 후 한국어 `TMP_SubMesh` 레이어를 부모 `TextMeshPro` 레이어와 동기화하도록 구성했습니다.
- 게임 0.6.21의 원본 관리 코드에서 텍스트 갱신, TMP 메시 생성, UI 레이어 변경, 카드 풀 반환 순서를 대조했습니다.
- 실제 게임 기동에서 플러그인 0.1.1, 카드 글자 레이어 복구 훅, 번역 2,735개와 한글 글리프 11,320개의 로드를 확인했습니다. Harmony 경고와 플러그인 예외는 발생하지 않았습니다.
- 플러그인 Release 빌드와 설치 ZIP 검증을 통과했습니다.

대상은 Windows x64, 게임 0.6.21 / Steam 빌드 25221077, Unity 2022.3.62f3입니다.

## 통과한 검사

- 영어 테이블 2,812개 중 한국어 2,735개, 명시적 원문 유지 77개. 누락 0개.
- JSON 중복 키, 알 수 없는 키, 수치·게임 토큰 수, 툴팁 링크 ID, 알려진 카드 참조 이름 검사.
- 한국어 조사와 카드 단위 검사 10개. ‘대상에게 5 피해’, 전체 대상, 명시적인 자신, 토큰만 있는 효과, 토스티 단수·복수와 0을 포함합니다.
- 플러그인 Release 빌드: 경고 0개, 오류 0개.
- 공식 BepInEx 755로 캐시와 Unity 라이브러리가 없는 상태에서 최초 다운로드·interop 생성·게임 진입 성공.
- 게임 로그에서 번역 2,735개와 글꼴 11,320자 로드 확인. 카드 이름·효과·일시 정지 메뉴의 한글 표시 확인.
- 최종 수정본의 공격 카드 목록에서 ‘굽기: 대상에게 3 피해’, ‘찌르기: 대상에게 4 피해’, ‘플래시: 대상에게 취약 3 추가’와 ‘처치 시, 토스티 생성’을 실기 확인.
- 번역에서 실제 사용한 한글 음절 853개가 모두 배포 글꼴에 포함되는지 확인.
- 실제 첫 실행에 사용한 로더와 공식 배포본의 파일 227개가 동일함을 해시로 확인.

## 남은 확인과 알려진 문제

- 모든 카드 조합, 후반 대화, 화면 비율을 플레이로 검증하지 않았습니다. 데이터 누락 0개가 모든 화면의 완벽한 번역을 의미하지는 않습니다.
- 창 크기를 강제로 변경한 테스트에서 카드 목록의 툴팁이 화면 아래에 잘리는 경우를 관찰했습니다. 일반 해상도별 배치 검증이 더 필요합니다.
- 원문 `Card/FORESIGHT_GOOD_TASTE`의 `Warm Butter` 참조는 현재 카드 이름 테이블에 대응 항목이 없어 정적 카드 미리보기 연결을 검증하지 못했습니다.
- BepInEx에서 `Class::Init signatures ... using a substitute` 경고가 발생하지만 테스트에서는 로드와 게임 진입을 완료했습니다.
- 업데이트로 원문이 바뀌면 해당 문구는 원문으로 표시됩니다. 다른 버전과 다른 모드의 조합은 검증하지 않았습니다.

설치 ZIP에는 테스트 로그, 화면 캡처, 계정·세션 식별자, 저장 파일, 게임 원본과 생성 interop 파일을 넣지 않습니다.

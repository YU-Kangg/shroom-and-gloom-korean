# 검증 기록 — v0.1.3-beta.1

## 0.6.39 호환성 갱신

- Steam 빌드 25342632에서 영어 테이블 2,826개를 다시 추출했습니다.
- 이전 버전 대비 새 키 15개, 삭제 키 1개, 원문 변경 키 2개를 개별 검토했습니다.
- 한국어 2,749개와 명시적 원문 유지 77개를 수록했고 누락 및 구조 검사는 0건입니다.
- 구버전 interop 캐시를 비운 뒤 최신 `GameAssembly.dll`에서 interop 159개를 다시 생성했습니다.
- 0.1.3 플러그인이 새 interop에서 로드되고 번역 2,749개와 한글 글꼴이 적용되는 것을 로그로 확인했습니다.
- 0.1.3 플러그인의 빌드 및 설치 패키지 검증을 통과했습니다.

## 현재 릴리스

- 강화 안내와 상점 보유량 문장은 한글과 숫자를 동일한 폰트 및 재질로 표시합니다. 숫자와 소수점 글리프가 번들에 포함된 것을 확인했습니다.
- 해당 수정이 포함된 dev.4를 적용한 사용자가 2026-09-12 정상 동작을 확인하고 릴리스를 요청했습니다. 모든 강화 종류 및 장시간 플레이를 개별 검증한 결과는 아닙니다.
- 개구리 툴팁은 마우스를 올린 버튼의 실제 망각 인수를 읽습니다. 회복 효과 문구는 ‘이 카드의 회복량 증가’로 명확히 했습니다.
- 반복 검색 및 상세 글리프 진단 코드는 배포 소스에서 제거했습니다. dev.4의 표시 동작은 유지합니다.
- 아래 내용은 이전 버전과 개발 중 조사 이력입니다. 중간 단계의 ‘미해결/미출시’ 기록은 당시 상태를 의미합니다.

## 카드 글자 표시 수정

- `TextMeshPro.GenerateTextMesh` 실행 후 한국어 `TMP_SubMesh` 레이어를 부모 `TextMeshPro` 레이어와 동기화하도록 구성했습니다.
- 게임 0.6.21의 원본 관리 코드에서 텍스트 갱신, TMP 메시 생성, UI 레이어 변경, 카드 풀 반환 순서를 대조했습니다.
- 실제 게임 기동에서 플러그인 0.1.1, 카드 글자 레이어 복구 훅, 번역 2,735개와 한글 글리프 11,320개의 로드를 확인했습니다. Harmony 경고와 플러그인 예외는 발생하지 않았습니다.
- 플러그인 Release 빌드와 설치 ZIP 검증을 통과했습니다.

현재 대상은 Windows x64, 게임 0.6.39 / Steam 빌드 25342632, Unity 2022.3.62f3입니다.

## 통과한 검사

- 영어 테이블 2,826개 중 한국어 2,749개, 명시적 원문 유지 77개. 누락 0개.
- JSON 중복 키, 알 수 없는 키, 수치·게임 토큰 수, 툴팁 링크 ID, 알려진 카드 참조 이름 검사.
- 한국어 조사와 카드 단위 검사 10개. ‘대상에게 5 피해’, 전체 대상, 명시적인 자신, 토큰만 있는 효과, 토스티 단수·복수와 0을 포함합니다.
- 플러그인 Release 빌드: 경고 0개, 오류 0개.
- 공식 BepInEx 755로 캐시와 Unity 라이브러리가 없는 상태에서 최초 다운로드·interop 생성·게임 진입 성공.
- 게임 로그에서 번역 2,735개와 글꼴 11,320자 로드 확인. 카드 이름·효과·일시 정지 메뉴의 한글 표시 확인.
- 최종 수정본의 공격 카드 목록에서 ‘굽기: 대상에게 3 피해’, ‘찌르기: 대상에게 4 피해’, ‘플래시: 대상에게 취약 3 추가’와 ‘처치 시, 토스티 생성’을 실기 확인.
- 번역에서 실제 사용한 한글 음절 853개가 모두 배포 글꼴에 포함되는지 확인.
- 실제 첫 실행에 사용한 로더와 공식 배포본의 파일 227개가 동일함을 해시로 확인.

## 남은 확인과 알려진 문제

- 모든 카드 조합, 후반 대화, 화면 비율을 플레이로 검증하지 않았습니다. 데이터 누락 0개가 모든 화면의 완벽한 번역을 의미하지는 않습니다.
- 창 크기를 강제로 변경한 테스트에서 카드 목록의 툴팁이 화면 아래에 잘리는 경우를 관찰했습니다. 일반 해상도별 배치 검증이 더 필요합니다.
- 원문 `Card/FORESIGHT_GOOD_TASTE`의 `Warm Butter` 참조는 현재 카드 이름 테이블에 대응 항목이 없어 정적 카드 미리보기 연결을 검증하지 못했습니다.
- BepInEx에서 `Class::Init signatures ... using a substitute` 경고가 발생하지만 테스트에서는 로드와 게임 진입을 완료했습니다.
- 업데이트로 원문이 바뀌면 해당 문구는 원문으로 표시됩니다. 다른 버전과 다른 모드의 조합은 검증하지 않았습니다.

설치 ZIP에는 테스트 로그, 화면 캡처, 계정·세션 식별자, 저장 파일, 게임 원본과 생성 interop 파일을 넣지 않습니다.

## 2026-09-12 추가 제보 조사 (0.1.2-dev, 미출시)

- 상점 개구리: `BowlShopEncounterUI.ForgetTrainingTypeForResource`는 지불 자원의 덱으로 설명을 선택하지만, 실제 버튼은 별도 `HandleModifyCardClick` 인수를 사용한다. 로컬 설치본의 P1/P2 CuriousMole 버튼은 탐험 카드, BattleMole 버튼은 전투 카드 망각에 연결된 것을 확인했다. 패치는 활성 버튼의 동일 인수를 읽고, 설정을 판별할 수 없으면 원래 동작을 유지한다. 실제 게임 화면 검증은 남아 있다.
- `INTENT_INCREASE_SELF_HEALING`: 회복량이 증가하는 대상을 명확하게 하도록 ‘이 카드의 회복량 증가’로 수정했다. 원문에 없는 수치 토큰은 추가하지 않았다.
- 강화 안내와 상점 보유 수량의 숫자 누락은 아직 미해결이다. 강화 안내 번역에는 숫자 2가 있고, 상점은 계산한 수량을 PNUM으로 전달한다. 폰트/렌더러 문제인지 확인하려면 문제가 발생한 화면의 실행 기록이 필요하다.
- 설정 파일의 `[Diagnostics] LogNumericText = true`는 한글과 숫자가 포함된 텍스트의 글리프, 재질, 투명도, 레이어를 기록한다. 기본값은 false이며 한 실행에서 최대 128개 문장만 기록한다. 로그는 `BepInEx/LogOutput.log`에 저장된다. 게임을 재실행하면 이전 로그가 교체되므로 문제 발생 직후 보관한다.
- 번역 카탈로그 검증: 누락 0, 오류 0. 문법 검사 10건 통과. 플러그인 빌드: 경고 0, 오류 0. 이는 숫자 누락 해결이나 전체 플레이 검증을 의미하지 않는다.

### 후속 제보와 dev.2 변경

- dev 버전에서도 탐험 망각 선택 화면과 전투 망각 툴팁이 동시에 표시됐다. 최초의 `ForgetTrainingTypeForResource` 후킹은 사용자 화면에서 효과를 확인하지 못했다. dev.2는 `Tooltip.GetTooltip`에서 해당 툴팁과 같은 오브젝트의 버튼에 연결된 실제 망각 인수를 읽는다. 다른 툴팁의 동작은 유지한다. 후킹 실패의 정확한 실행 경로는 아직 확정하지 않았다.
- `Tooltip.GetTooltip`은 localized 문구를 static 문구보다 우선한다. 다만 조사한 상점 프리팹의 localized 값은 비어 있었으므로, 이것이 이번 현상의 직접 원인이라고 단정할 수 없다.
- 음식 제보 이미지에서는 좌측 상단 `:`와 `0`이 보이고 한글이 보이지 않는다. 이는 강화 안내에서 숫자만 빠지는 제보와 구별한다. `0`이 실제 보유량과 일치하는지는 화면만으로 확인할 수 없다.
- 최초의 숫자 진단은 일반 카드와 색상 태그 때문에 128개 한도에 먼저 도달했다. 따라서 상점 기록이 없다는 사실만으로 상점 화면을 재현하지 않았다고 판단할 수 없다. dev.2의 별도 관찰기는 2초마다 활성 상점 보유량 및 강화 상세 텍스트의 상태를 읽고, 상태가 달라졌을 때 기록한다. 실제 화면의 텍스트, 글리프 색상, 렌더러, 카메라 마스크를 포함하며 렌더링 상태를 변경하지 않는다.
- dev.2는 빌드 검증을 마쳤으나 실제 상점/강화 화면에서 검증하기 전이다. 숫자 및 한글 누락은 미해결 상태다.

### 진단 버전 중단 및 로컬 복구

- dev.3에서 사용자가 반복 멈춤을 제보했다. 확인 시 게임 프로세스는 종료돼 있었으며, 확보한 로그만으로 직접 원인을 확정하지 못했다. 주기적인 전체 오브젝트 검색 및 렌더링 정보 수집은 최근 추가한 부하 요인이다. dev.3를 안정적이라고 취급하거나 배포하지 않는다.
- 재현 로그와 dev.3 DLL을 로컬 `.work/freeze-report-*`에 보관했다. 설치된 DLL은 진단 추가 전 0.1.1 백업으로 복구하고 해시 일치를 확인했다. `LogNumericText`도 false로 변경했다. 수정 중인 소스와 번역 문구는 보존했다.
- 복구 후 게임 플레이 안정성은 아직 검증하지 않았다. 개구리 툴팁 수정은 설치된 DLL에서 빠졌으며, 숫자/한글 누락 문제도 미해결이다. 새 릴리스는 게시하지 않았다.

### dev.4 표시 우회 수정 후보

- 사용자가 0.1.1에서 허수아비 강화 수치 누락을 다시 보고했다. dev.4는 강화 상세 안내와 상점 보유량 문장에 한글/숫자 공용 폰트 및 재질을 적용해 폰트별 렌더러 분리를 피한다. 번들에 숫자와 소수점 글리프가 포함된 것을 확인했다.
- 주기적 진단기와 글리프 진단 호출은 설정과 무관하게 꺼 둔다. 반복 오브젝트 검색을 실행하지 않는다.
- 빌드 통과. 현재는 표시 문제의 우회 수정 후보이며, 게임 화면에서 효과 및 안정성을 검증하지 않았다. 설치/배포 완료로 간주하지 않는다.
